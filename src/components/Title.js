import { Box } from "@mui/material";

const Title = () => {
    return (
        <Box>
            <h1>Imposify</h1>
            <h2>the free book imposition tool</h2>
        </Box>
    );
}

export default Title;